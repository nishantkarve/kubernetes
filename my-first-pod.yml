apiVersion: v1
kind: Pod
metadata:
  name: my-first-pod
  labels:
    app: my-first-app
spec:
  containers:
  - name: my-firstcontainer
    image: busybox
    command: ['sh','-c','echo Hello Kubernetes && sleep 3600']